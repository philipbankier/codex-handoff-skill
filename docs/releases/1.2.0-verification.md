# 1.2.0 Release Verification

## Commands

| Command | Exit Code | Result |
|---------|-----------|--------|
| `bash scripts/verify-install.sh` | 0 | Passed. Codex CLI found as `codex-cli 0.142.0-alpha.1`; Claude Code and OpenClaw symlinks point at this checkout. |
| `bash scripts/verify-release.sh` | 0 | Passed. Skill size 4807 bytes, command file 10 lines, frontmatter present, shell syntax OK, version checks OK, plugin/eval/release files present, JSON parse OK, stale workflow scan OK, secret scan OK. |
| `git diff --check` | 0 | Passed with no output. |
| `LC_ALL=C` broad ASCII scan | 0 | Passed with no output across README, changelog, OpenClaw manifest, commands, scripts, skills, plugins, evals, docs, examples, and implementation planning notes. |
| Required broad stale-literal scan | 0 | The command completed, but reported only the off-limits untracked release plan under `docs/superpowers/plans/`. No tracked files were reported. |

## Known Limitations

- Codex-native plugin install should be verified against the installed Codex CLI with `codex plugin --help` and `codex plugin marketplace --help`.
- Grok and GLM worker lanes are not part of 1.2.0.
- Claude plugin packaging is deferred.
- The broad stale-literal scan includes untracked `docs/superpowers/plans/`, which Task 6 explicitly forbids editing. That file remains outside the release diff.
- Philip approved treating `docs/superpowers/plans/` as off-limits planning input rather than release output for this scan.
- The stale-literal scan command and its matched retired literals are not restated verbatim in this file because this file is itself inside the scanned `docs` path.

## Notes

Task 6 stores final release evidence here so reviewers can see command exits and concrete limits without rerunning the full checklist.
